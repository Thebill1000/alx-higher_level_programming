#!/usr/bin/node
/* a script that prints 3 lines by using an array of string and loop */
const array = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < array.length; i++) {
  console.log(array[i]);
}
